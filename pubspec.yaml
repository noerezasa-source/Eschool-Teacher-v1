name: eschool_saas_staff
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.5+13

environment:
  sdk: ">=3.3.1 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.2

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.6
  flutter_bloc: ^8.1.6
  dio: ^5.6.0
  cached_network_image: ^3.4.1
  get: ^4.6.6
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  # firebase_core: ^2.24.2
  # firebase_auth: ^4.15.3
  # firebase_messaging: ^14.0.0
  # firebase_auth_web: ^5.8.13
  # firebase_messaging_web: ^3.5.18
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  firebase_messaging: ^16.1.3
  firebase_auth_web: ^6.1.4
  firebase_messaging_web: ^4.1.4
  flutter_local_notifications: ^17.1.0
  google_fonts: ^8.0.2
  flutter_svg: ^2.2.0
  flutter_widget_from_html: ^0.15.1
  url_launcher: ^6.3.0
  intl: ^0.20.2
  timeago: ^3.7.0
  file_picker: ^8.1.2
  permission_handler: ^11.3.1
  device_info_plus: ^9.1.0
  path_provider: ^2.1.4
  readmore: ^3.0.0
  responsive_builder: ^0.7.1
  open_filex: ^4.5.0
  shared_preferences: ^2.0.6
  package_info_plus: ^8.0.2
  web_socket_channel: ^3.0.1
  image_picker: ^1.1.2
  video_player_android: ^2.7.1
  flutter_launcher_icons: ^0.14.2
  auto_size_text: ^3.0.0
  flutter_screenutil: ^5.9.3
  month_picker_dialog: ^5.1.3
  bloc: any
  flutter_localization: ^0.2.3
  shimmer: ^2.0.0
  animate_do: ^3.0.2
  flutter_animate: ^4.5.0
  glassmorphism_widgets: ^0.2.2
  glassmorphism: ^3.0.0
  glass_kit: ^3.0.0
  simple_animations: ^5.0.0+3
  chewie: ^1.12.1
  html: ^0.15.0
  flutter_staggered_grid_view: ^0.7.0
  flutter_staggered_animations: ^1.1.1
  lottie: ^2.7.0
  animations: ^2.0.8
  font_awesome_flutter: ^10.8.0
  animated_text_kit: ^4.2.2
  animated_card: ^2.0.0
  confetti: ^0.8.0
  flutter_typeahead: ^5.2.0
  equatable: ^2.0.7
  flutter_image_compress: ^2.3.0
  path: ^1.9.0
  image: ^4.2.0
  vibration: ^3.1.4
  http_parser: ^4.1.0

  # ============ TAMBAHAN BARU ============
  # Dibutuhkan oleh flutter_bloc agar context.read() dan context.watch() bisa berjalan
  provider: ^6.1.2

  # Dibutuhkan untuk method firstWhereOrNull() yang dipakai di banyak file
  collection: ^1.18.0

  # Dibutuhkan oleh chewie agar VideoPlayerController bisa dikenali
  video_player: ^2.9.1

  # Dibutuhkan untuk AudioPlayer di fwfh_just_audio
  just_audio: ^0.9.38

  # Dibutuhkan oleh flutter_cache_manager untuk database lokal
  sqflite: ^2.3.3+1

  # PERBAIKAN UTAMA: Mengganti open_settings yang eror lStar dengan package yang up-to-date
  app_settings: ^6.0.0

dependency_overrides:
  webview_flutter_android: ^3.16.0
  webview_flutter_platform_interface: ^2.10.0

# dependency_overrides:
#   chewie:
#     git:
#       url: https://github.com/mdeimert/chewie.git
#       ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  change_app_package_name: ^1.1.0

# flutter_launcher_icons:
#   image_path_android: "assets/icon/eschool.png"
#   image_path_ios: "assets/icon/eschool.png"
#   android: true
#   ios: true
#   adaptive_icon_background: "#ffffff"
#   adaptive_icon_foreground: "assets/icon/eschool.png"
#   min_sdk_android: 21

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:

  assets:
    - assets/images/
    - assets/icon/
    - google_fonts/
    - assets/languages/
    # - assets/animations/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages